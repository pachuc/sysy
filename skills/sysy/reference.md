# sysy command reference

Generated by `scripts/update-skill-reference.sh`. Do not edit by hand.

## `sysy`

```text
System designs as data

Usage: sysy [OPTIONS] <COMMAND>

Commands:
  version    Print the version of this CLI
  new        Create an empty design without overwriting an existing file
  node       Add, edit, remove, or list nodes
  container  Add, edit, remove, or list containers
  edge       Add, edit, remove, or list edges
  note       Add, edit, remove, or list notes
  show       Print the whole design
  validate   Check the design and report all problems
  help       Print this message or the help of the given subcommand(s)

Options:
      --json     Print machine-readable JSON instead of text
  -h, --help     Print help
  -V, --version  Print version
```

## `sysy version`

```text
Print the version of this CLI

Usage: sysy version [OPTIONS]

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy new`

```text
Create an empty design without overwriting an existing file

Usage: sysy new [OPTIONS] --title <TITLE> <PATH>

Arguments:
  <PATH>

Options:
      --json                       Print machine-readable JSON instead of text
      --title <TITLE>
      --description <DESCRIPTION>
  -h, --help                       Print help
```

## `sysy node`

```text
Add, edit, remove, or list nodes

Usage: sysy node [OPTIONS] <COMMAND>

Commands:
  add     Add a node
  set     Change supplied fields, preserving all others
  remove  Remove the node, its edges, attached notes, and their layout entries
  list    List records sorted by id
  help    Print this message or the help of the given subcommand(s)

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy node add`

```text
Add a node

Usage: sysy node add [OPTIONS] --kind <KIND> --label <LABEL> <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json                       Print machine-readable JSON instead of text
      --kind <KIND>                Component kind [possible values: service, database, queue, cache, storage, client, external, function, generic]
      --label <LABEL>              Display label
      --description <DESCRIPTION>  Description
      --tag <TAG>                  Tags (repeat to supply several; set replaces all tags)
      --in <CONTAINER>             Containing container
  -h, --help                       Print help
```

## `sysy node set`

```text
Change supplied fields, preserving all others

Usage: sysy node set [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json                       Print machine-readable JSON instead of text
      --kind <KIND>                Component kind [possible values: service, database, queue, cache, storage, client, external, function, generic]
      --label <LABEL>              Display label
      --description <DESCRIPTION>  Description
      --tag <TAG>                  Tags (repeat to supply several; set replaces all tags)
      --in <CONTAINER>             Containing container
      --clear-description          Clear description
      --clear-in                   Move to the top level
      --clear-tags                 Clear tags
  -h, --help                       Print help
```

## `sysy node remove`

```text
Remove the node, its edges, attached notes, and their layout entries

Usage: sysy node remove [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy node list`

```text
List records sorted by id

Usage: sysy node list [OPTIONS] <PATH>

Arguments:
  <PATH>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy container`

```text
Add, edit, remove, or list containers

Usage: sysy container [OPTIONS] <COMMAND>

Commands:
  add     Add a container
  set     Change supplied fields, preserving all others
  remove  Reparent children and remove the container, its edges, and attached notes
  list    List records sorted by id
  help    Print this message or the help of the given subcommand(s)

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy container add`

```text
Add a container

Usage: sysy container add [OPTIONS] --label <LABEL> <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json                       Print machine-readable JSON instead of text
      --label <LABEL>              Display label
      --description <DESCRIPTION>  Description
      --in <PARENT>                Parent container
  -h, --help                       Print help
```

## `sysy container set`

```text
Change supplied fields, preserving all others

Usage: sysy container set [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json                       Print machine-readable JSON instead of text
      --label <LABEL>              Display label
      --description <DESCRIPTION>  Description
      --in <PARENT>                Parent container
      --clear-description          Clear description
      --clear-in                   Move to the top level
  -h, --help                       Print help
```

## `sysy container remove`

```text
Reparent children and remove the container, its edges, and attached notes

Usage: sysy container remove [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy container list`

```text
List records sorted by id

Usage: sysy container list [OPTIONS] <PATH>

Arguments:
  <PATH>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy edge`

```text
Add, edit, remove, or list edges

Usage: sysy edge [OPTIONS] <COMMAND>

Commands:
  add     Add an edge
  set     Change supplied fields, preserving all others
  remove  Remove the edge, attached notes, and their layout entries
  list    List records sorted by id
  help    Print this message or the help of the given subcommand(s)

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy edge add`

```text
Add an edge

Usage: sysy edge add [OPTIONS] --from <FROM> --to <TO> <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --from <FROM>    Source node or container
      --json           Print machine-readable JSON instead of text
      --to <TO>        Destination node or container
      --kind <KIND>    Line kind (defaults to sync on add) [default: sync] [possible values: sync, async, data, dependency]
      --label <LABEL>  Display label
      --bidirectional  Draw arrows in both directions
  -h, --help           Print help
```

## `sysy edge set`

```text
Change supplied fields, preserving all others

Usage: sysy edge set [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --from <FROM>
          Source node or container
      --json
          Print machine-readable JSON instead of text
      --to <TO>
          Destination node or container
      --kind <KIND>
          Line kind (defaults to sync on add) [possible values: sync, async, data, dependency]
      --label <LABEL>
          Display label
      --bidirectional[=<BIDIRECTIONAL>]
          Draw arrows in both directions; use --bidirectional=false to disable [possible values: true, false]
      --clear-label
          Clear label
  -h, --help
          Print help
```

## `sysy edge remove`

```text
Remove the edge, attached notes, and their layout entries

Usage: sysy edge remove [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy edge list`

```text
List records sorted by id

Usage: sysy edge list [OPTIONS] <PATH>

Arguments:
  <PATH>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy note`

```text
Add, edit, remove, or list notes

Usage: sysy note [OPTIONS] <COMMAND>

Commands:
  add     Add a note
  set     Change supplied fields, preserving all others
  remove  Remove the note and its layout entry
  list    List records sorted by id
  help    Print this message or the help of the given subcommand(s)

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy note add`

```text
Add a note

Usage: sysy note add [OPTIONS] --text <TEXT> <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json         Print machine-readable JSON instead of text
      --text <TEXT>  Note text
      --on <ON>      Node, container, or edge to attach to
  -h, --help         Print help
```

## `sysy note set`

```text
Change supplied fields, preserving all others

Usage: sysy note set [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json         Print machine-readable JSON instead of text
      --text <TEXT>  Note text
      --on <ON>      Node, container, or edge to attach to
      --clear-on     Detach the note
  -h, --help         Print help
```

## `sysy note remove`

```text
Remove the note and its layout entry

Usage: sysy note remove [OPTIONS] <PATH> <ID>

Arguments:
  <PATH>
  <ID>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy note list`

```text
List records sorted by id

Usage: sysy note list [OPTIONS] <PATH>

Arguments:
  <PATH>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy show`

```text
Print the whole design

Usage: sysy show [OPTIONS] <PATH>

Arguments:
  <PATH>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```

## `sysy validate`

```text
Check the design and report all problems

Usage: sysy validate [OPTIONS] <PATH>

Arguments:
  <PATH>

Options:
      --json  Print machine-readable JSON instead of text
  -h, --help  Print help
```
